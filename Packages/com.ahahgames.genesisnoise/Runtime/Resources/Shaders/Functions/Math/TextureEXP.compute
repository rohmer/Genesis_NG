#pragma kernel CSMain

Texture2D<float4> inputA;
RWTexture2D<float4> output;

[numthreads(8, 8, 1)]
void CSMain(uint3 id : SV_DispatchThreadID)
{
    uint2 coord = id.xy;
    output[coord] = exp(inputA[coord]);
}